let arr = [ 21, 43, 77, 66, 12, 7, 48];

for ( let i = 0; i < arr.length; i++){
    if ( arr[i] % 2 === 0){
        console.log(`These are the even number${arr[i]}`);
    }
    else{
    console.log(`These are the odd number${arr[i]}`);
}
}
/* let arr = [21, 43, 77, 66, 12, 7, 48];
let evens = [];
let odds = [];

for (let i = 0; i < arr.length; i++) {
    if (arr[i] % 2 === 0) {
        evens.push(arr[i]);
    } else {
        odds.push(arr[i]);
    }
}

console.log(`These are the even numbers: ${evens}`);
console.log(`These are the odd numbers: ${odds}`);   */

